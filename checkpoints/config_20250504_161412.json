{
    "epochs": 100,
    "batch_size": 128,
    "learning_rate": 0.1,
    "weight_decay": 0.0005,
    "dataset": "cifar10",
    "model_size": "medium",
    "data_dir": "./data",
    "save_dir": "./checkpoints",
    "early_stopping_patience": 10,
    "use_amp": true
}